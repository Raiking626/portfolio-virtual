---
import { softSkills } from "../../../cv.json";
---
<h3 class="mb-2">Habilidades blandas</h3>
<ul class="flex flex-row gap-6 text-center items-center flex-wrap">
  {softSkills.map(e => (
    <li class="rounded border border-white bg-white text-gray-700 px-1">{e.name}</li>
  ))} 
</ul>